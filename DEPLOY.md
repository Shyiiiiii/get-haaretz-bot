# הקמה — מסלול Twilio (הפשוט)

בלי יצירת אפליקציה ב-Meta, בלי Token של WhatsApp. הבוט עונה ישירות.
שני חשבונות חינמיים בלבד: **Render** (אירוח) ו-**Twilio** (חיבור לוואטסאפ).

---

## שלב 1 — העלה את הקוד לאוויר (Render)

הבוט צריך כתובת אינטרנט ציבורית. הכי פשוט — GitHub + Render, בלי שורת פקודה:

**א. העלה ל-GitHub (דרך הדפדפן):**
1. https://github.com/new → צור repo חדש (שם: `get-haaretz-bot`), לחץ Create.
2. במסך שנפתח: **uploading an existing file** → גרור לתוכו את **כל הקבצים מהתיקייה** (בלי node_modules) → Commit.

**ב. פרוס ב-Render:**
1. https://render.com/ → הרשמה (אפשר עם GitHub).
2. **New → Blueprint** → בחר את ה-repo. Render יזהה את `render.yaml`.
3. **Apply**. בסיום תקבל כתובת כמו `https://get-haaretz-bot.onrender.com`.
   בדוק שהיא עובדת — פתח אותה בדפדפן, אמור להופיע "Get הארץ pricing bot ✅".

> אם יש לך מפתח Google Maps — הזן אותו כ-`GOOGLE_MAPS_API_KEY`. לא חובה: כל מסלולי הליבה מתומחרים גם בלעדיו.

---

## שלב 2 — חבר לוואטסאפ (Twilio Sandbox, ~5 דק')

1. https://www.twilio.com/try-twilio → פתח חשבון חינם.
2. בקונסולה: **Messaging → Try it out → Send a WhatsApp message**.
3. תראה מספר של Twilio ומילת קוד, למשל: **join <קוד>**.
   שלח מהוואטסאפ שלך את ההודעה `join <קוד>` למספר שמופיע → תקבל אישור הצטרפות.
4. באותו מסך, לשונית **Sandbox settings**:
   - בשדה **"When a message comes in"** הדבק:
     `https://get-haaretz-bot.onrender.com/whatsapp`
   - Method: **POST** → **Save**.

---

## שלב 3 — בדיקה 🎉

מהוואטסאפ שלך (זה שהצטרף ל-sandbox), שלח למספר של Twilio:
```
מבית שמש לירושלים
```
תוך שנייה תקבל הצעת מחיר של **150₪** ✅

נסה גם: `בב פת` (יחזיר 80) · `ים שמש הלוך חזור` · `מבית שמש לרמות` (170).

---

---

## לוח הניהול 📊

אחרי הפריסה, פתח בדפדפן:
```
https://הכתובת-שלך.onrender.com/admin?pass=הסיסמה-שבחרת
```

מה יש שם:
- **כל הפניות** שהגיעו לבוט — טלפון, ההודעה, המסלול, המחיר שנקבע והרכב
- **סטטיסטיקה** — פניות היום/השבוע, לקוחות ייחודיים, מסלולים מבוקשים
- **עריכת מחירים** — משנים מחיר ולוחצים שמור. בלי לגעת בקוד.

> **חשוב:** שנה את הסיסמה. ב-Render → Environment → הוסף משתנה `ADMIN_PASS` עם סיסמה משלך.

---

## מה הבוט יודע לעשות

| הלקוח שולח | הבוט מבין |
|-----------|-----------|
| `מבית שמש לירושלים` | מסלול מלא → 150₪ |
| `בב פת` | קיצורי סדרנים → 80₪ |
| `מבית שמש לרמות` | שכונות ירושלים → 170₪ |
| `ים שמש הלוך חזור` | נסיעה הלוך-חזור → כפול |
| `מבית שמש לירושלים ל-12 אנשים` | מספר נוסעים → מיניבוס, 300₪ |
| `מתל אביב לירושלים אוטובוס` | סוג רכב → אוטובוס תיירים |
| `פנימי בב` | נסיעה עירונית → הפניה לתיאום |

---

## אבטחה — שני משתנים שחשוב להגדיר ב-Render

| משתנה | למה |
|-------|-----|
| `ADMIN_PASS` | סיסמה משלך ללוח הניהול. בלעדיה הסיסמה היא ברירת מחדל ידועה, וכל מי שינחש רואה את כל טלפוני הלקוחות. |
| `TWILIO_AUTH_TOKEN` | מ-Twilio Console. מוודא שרק Twilio יכולה לשלוח לבוט — בלעדיו כל אחד יכול להזריק פניות מזויפות. |

השרת מדפיס אזהרה בהפעלה אם הם חסרים.

---

## גיבוי פניות ⚠️

הפניות נשמרות בקובץ על השרת. **ב-Render חינמי הקובץ נמחק בכל פריסה מחדש.**
בלוח הניהול יש כפתור **⬇️ ייצוא ל-CSV** — כדאי לייצא מדי פעם.

---

## הערות

- **Sandbox** מיועד לבדיקה: כל מי שרוצה לקבל תשובות צריך לשלוח פעם אחת `join <קוד>`, והחיבור מתחדש כל 3 ימים.
- **לפרודקשן** (שכל לקוח יוכל לכתוב בלי join): ב-Twilio → **Messaging → Senders → WhatsApp senders** → רישום המספר העסקי 0506567045. Twilio מטפל מול Meta בשבילך — הרבה יותר פשוט מלעשות זאת לבד.
- אין צורך בתבניות (templates): הבוט תגובתי, עונה בתוך חלון 24 השעות.
- גרסה זו משתמשת ב-Twilio כמסלול ההפעלה הראשי.

## בדיקה מקומית (רשות)
```bash
npm ci
npm test
npm run simulate     # שיחה עם הבוט בטרמינל, ללא מפתחות
```

---

## המחירון (לשימוש שלך) 📋

הקובץ **`מחירון-Get-הארץ.html`** — כלי חיפוש מחיר מהיר, לא קשור לבוט.

**איך משתמשים:** פותחים את הקובץ בכרום/ספארי (דאבל-קליק). עובד **גם בלי אינטרנט**.
מומלץ: בנייד → שיתוף → "הוסף למסך הבית" → נפתח כמו אפליקציה.

**מה יש בו:**
- הקלדה עם השלמה אוטומטית (98 ערים + 34 שכונות ירושלים)
- מחיר מיידי, כולל תעריף שכונתי (רמות 170 מול ירושלים 150)
- בחירת רכב (פרטי עד אוטובוס) והלוך-חזור — המחיר מתעדכן
- מסלולים נפוצים בלחיצה אחת
- כפתור שליחה בוואטסאפ עם המחיר מוכן ללקוח
- כל מחיר מסומן: **המחירון שלך** או **מחיר שוק** (עם מספר דגימות ואחוז הסכמה)

**עדכון אחרי שינוי מחירים:**
```bash
npm run pricebook
```

---

## הסרת מסלול מהתמחור

ב-`data/manual-prices.json` יש רשימת **`excluded`**:

```json
"excluded": ["גבעת זאב → ירושלים"]
```

מסלול ברשימה **לא יתומחר** מאף מקור (המחירון שלך / מפורסם / שוק), בשני הכיוונים.
הלקוח יקבל: *"למסלול הזה אני קובע מחיר אישי"* + הפניה לטלפון.

ההחרגה שורדת גם בנייה מחדש של טבלת השוק. אחרי שינוי — `npm run pricebook`.

---

## העמוד הציבורי ללקוחות 🌐

הקובץ **`מחירון-ללקוחות.html`** — עמוד מונגש לשיתוף בקבוצות וואטסאפ ובפרופיל.

**מה יש בו:**
- בדיקת מחיר עצמאית (מוצא + יעד) — מיידי
- כפתור **"הזמנת נהג בוואטסאפ"** קבוע בתחתית, שנשלח עם המסלול והמחיר מוכנים
- מסלולים מבוקשים בלחיצה
- מסלול ללא מחיר → "בקשת מחיר" בוואטסאפ (ליד חם)
- מותאם לנייד, נפתח מצוין מתוך וואטסאפ

**חשוב:** מוצגים **רק מחירים שאישרת אישית** (`confirmed`). כך אינך מתחייב פומבית
למחירים שנגזרו מנתוני שוק. להוסיף מסלול לעמוד — אשר אותו במחירון.

### איך להעלות ולקבל קישור (2 דקות, חינם)

1. היכנסו ל-**https://app.netlify.com/drop**
2. גררו את הקובץ `מחירון-ללקוחות.html` לתוך העמוד
3. תקבלו קישור מיידי, למשל `https://xyz123.netlify.app`
4. שתפו אותו בקבוצות, בסטטוס ובפרופיל העסקי

> לשם יפה יותר: ב-Netlify → Site settings → Change site name → למשל `get-haaretz`.

**עדכון המחירון בעמוד:** `npm run build` → העלו שוב את הקובץ ל-Netlify.
